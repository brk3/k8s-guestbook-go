#image: docker:git
#services:
#  - docker:dind

#build-job:
  #stage: build
  #script:
    #- cd guestbook-go
    ##- REGISTRY=brk3/guestbook-go make build
    #- docker build -t "brk3/guestbook" .

#push-job:
  #stage: build
  #script:
    #- echo "todo..."
    ##- REGISTRY=brk3/guestbook-go make push

 deploy:
   image:
     name: bitnami/kubectl:latest
     entrypoint: [""]
   script:
   - kubectl config get-contexts
   - kubectl config use-context mygroup1371/myproject:pbourke
   - kubectl get pods