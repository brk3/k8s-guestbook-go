build-job:
  stage: build
  script:
    - cd guestbook-go
    - REGISTRY=brk3/guestbook-go make build

push-job:
  stage: push
  script:
    - echo "todo..."
    #- REGISTRY=brk3/guestbook-go make push
